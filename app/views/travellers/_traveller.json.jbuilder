json.extract! traveller, :id, :quote_id, :policy_id, :first_name, :last_name, :birthdate, :age, :beneficiary, :travel_prem_id, :base_prem, :total_charges, :gross_prem, :created_at, :updated_at
json.url traveller_url(traveller, format: :json)
